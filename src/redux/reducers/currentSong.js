const initialState = {
    fileName: 'Artista - Título.mp3',
    blobFile: null,
    mp3File: null,
    onPlay: false,
    fgColor: '#ae12d4',
    bgColor: '#fff700'
}

const reducer = (state = initialState, action) => {
    return state;
}

export default reducer;